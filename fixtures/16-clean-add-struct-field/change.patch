--- a/src/model.rs
+++ b/src/model.rs
@@ -3,6 +3,7 @@ pub struct User {
     pub name: String,
     pub email: String,
     pub role: String,
+    pub avatar_url: Option<String>,
 }
 
 impl Default for User {
@@ -11,6 +12,7 @@ impl Default for User {
             name: String::new(),
             email: String::new(),
             role: "viewer".to_string(),
+            avatar_url: None,
         }
     }
 }
