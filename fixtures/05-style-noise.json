{
  "name": "style-noise",
  "description": "Code with inconsistent naming and formatting. Style-only issues that must NOT be flagged.",
  "diff": "--- a/src/utils.rs\n+++ b/src/utils.rs\n@@ -0,0 +1,15 @@\n+pub fn calculateTotal(items: &[f64]) -> f64 {\n+    let mut total = 0.0;\n+    for item in items {\n+        total += item;\n+    }\n+    total\n+}\n+\n+pub fn IS_VALID(input: &str) -> bool {\n+    !input.is_empty() && input.len() < 256\n+}\n+\n+pub fn   format_output(   data: &str   ) -> String {\n+    format!(\"[OUTPUT] {}\", data)\n+}\n",
  "files": {
    "src/utils.rs": "pub fn calculateTotal(items: &[f64]) -> f64 {\n    let mut total = 0.0;\n    for item in items {\n        total += item;\n    }\n    total\n}\n\npub fn IS_VALID(input: &str) -> bool {\n    !input.is_empty() && input.len() < 256\n}\n\npub fn   format_output(   data: &str   ) -> String {\n    format!(\"[OUTPUT] {}\", data)\n}\n"
  },
  "conventions": null,
  "expected_findings": []
}
